name: Deploy to FTP

on:
  push:
    branches:
      - main
  workflow_dispatch:
  repository_dispatch:
    types: [wp-build]

jobs:
  deploy:
    uses: hiep011094/shared-workflows/.github/workflows/deploy-ftp.yml@main
    with:
      server: jline
      server-dir: public_html/client/template_jline_html/
    secrets: inherit